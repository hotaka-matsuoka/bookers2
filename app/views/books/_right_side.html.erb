<div class="col-xs-9">
  <h2>Books</h2>
  <table class="table table-hover table-inverse">
    <thead>
      <tr>
        <th></th>
        <th>Title</th>
        <th>Opinion</th>
        <th colspan="3"></th>
      </tr>
    </thead>
    <tbody>
      <% @books.each do |book| %>
        <tr>
          <td>
            <% unless book.user.image %>
              <%= image_tag 'no_image.jpg', size: '50x50' %>
            <% else %>
              <%= attachment_image_tag(book.user, :image, :fill, 50, 50, format: "jpg")%>
            <% end %>
          </td>
          <td>
            <%= link_to "#{book.title}", book_path(book.id) %>
          </td>
          <td>
            <%= book.opinion %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>